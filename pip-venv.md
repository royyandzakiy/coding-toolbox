- Prep venv & requirements
```bash
python3 -m venv .venv

source .venv/bin/activate # linux
.\.venv\Scripts\activate # windows

pip install -r requirements.txt
```

- Upgrade pip & install
```bash
pip install --upgrade pip

pip install pytest pyserial
```